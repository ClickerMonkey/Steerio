- Target doesn't return Vec, it's passed as an out parameter and Target returns boolean
- Remove hard coded proximity filtering from all behaviors & targets, replace with filtering when necessary
- Remove hard coded field of view filtering from all behaviors & targets, replace with filtering when necessary
- Combine SpatialEntityObstacle and Obstacle
- Implement VecN
- Unit Testing
    interceptTime
    getDistanceFromLine